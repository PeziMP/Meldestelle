import React from 'react';
import ReactDOM from 'react-dom/client';
import TeilnehmerAnmeldung from './components/TeilnehmerAnmeldung';

const root = ReactDOM.createRoot(document.getElementById('root')!);

root.render(
  <React.StrictMode>
    <TeilnehmerAnmeldung />
  </React.StrictMode>
);

